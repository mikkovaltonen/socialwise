# Python requirements for MRP ETL Pipeline

# Core data processing
pandas==2.2.0
numpy==1.26.0
openpyxl==3.1.2          # For Excel file handling

# Firebase REST API
requests==2.31.0         # For Firestore REST API calls

# Environment variables
python-dotenv==1.0.0

# Optional: For even faster processing (recommended for large datasets)
# polars==0.20.0
# pyarrow==15.0.0
